services:
  frontend:
    build:
      context: ../..
      dockerfile: docker/local/Dockerfile
    ports:
      - "5173:5173"
    volumes:
      - ../../:/app
      - /app/node_modules
    networks:
      - internal
      - spa

networks:
  internal:
    driver: bridge
  spa:
    external: true  